{
  "name": "prep-os",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter @prep-os/web dev",
    "dev:api": "pnpm --filter @prep-os/api dev",
    "start:api": "pnpm --filter @prep-os/api start",
    "build": "pnpm -r build"
  }
}